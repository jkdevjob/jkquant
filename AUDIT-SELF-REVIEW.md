# jkquant 점검 기록 — 무한매수법 · VR

> ## 이번 문서 = **7차 점검 (통산 7번째 · 자체 점검 2차)** · 수정 **7-① ~ 7-⑱ (18건)** · PR **#246**
> 기준: main `02d6a9bf643f2cae1e75d67deac7fc9150272220` + 7차 수정
> 범위: 무한매수법·VR 중점 (7-①~⑭) + 섀넌·200일선·적립·ASAP (7-⑮~⑱)
> 범위 밖: 점검 도중 main 에 들어온 병렬 작업(5년 플랜 두 경로 v1.8.0 — PATH A 'TECL 역분산 + TQQQ Guard' 신규 계산 · 데이트레이딩·시초가)은
> 이번 대조에 넣지 않았다. 플랜의 무매·VR 경로(PATH B)는 함수가 그대로라 7차 수정이 그대로 얹혔고 회귀가 확인한다.
> 버전: index.html **v3.84.0** · backtest.html **v2.3.0** · plan.html **v1.8.1** · 서버 `functions/api/_im.js`·`autotrade.js`
> 회귀 **1827 PASS / 0 FAIL** (7차 교차 대조 `[105]`~`[115]` 포함) · strategy-check 22 PASS ·
> 변이 시험 **35종 전부 빨간불** (첫 판 20종 중 2종이 살아남아 값 시험을 넣고 다시 돌림 — 5장)

---

## 0. 점검 차수 — GPT 가 번호로 짚을 수 있게

| 차수 | 종류 | 날짜 | PR | 규모 |
|---|---|---|---|---|
| 1차 | GPT 감사 | 09-21 | #209 · #210 · #211 (+후속 #216 · #221 · #223 · #226) | ①~⑫ + same-close·리버스 후속 |
| 2차 | GPT 감사 | 09-22 | #229 | 15건 |
| 3차 | GPT 감사 | 09-22 | #232 | 12건 |
| 4차 | GPT 감사 | 09-23 | #234 | 20항목 |
| — | 자체 점검 1차 | 09-23 | (이 문서 초판) | N1~N6 |
| 5차 | GPT 감사 | 09-23 | #235 | 12항목 |
| — | 병렬 세션 | 09-23 | #236 | VR 사다리 되돌림 · 5년 플랜 동기화 |
| — | 자체 점검 1차 후속 | 09-23 | #237 N2 · #238 N1 · #239 UI | |
| **7차** | **자체 점검 2차 (이번)** | 09-23 | **#246** | **7-①~⑱** |

수정 번호는 `7-①` 처럼 **차수-번호** 로 적는다. 코드 주석·회귀 이름에도 `7차 ②` 처럼 같은 번호가 붙어 있다.

---

## 1. 결론 한 줄

**운영·모의·백테가 이제 같은 실데이터에서 거래 하나까지 같다.** 이번 점검 전에는 같지 않았다 —
가장 큰 것(7-②)은 모의·백테가 **쿼터매도 체결 뒤 바뀐 T 로 같은 종가에 다시 사는 룩어헤드**를
함께 갖고 있어서, 실전 흐름을 재현한 결과보다 최종자산이 **−14% ~ +17%** 틀려 있었다.
그런데도 회귀는 초록이었다 — 두 엔진이 **같은 식으로 틀렸고**, 회귀는 둘을 서로만 맞대 봤기 때문이다.

무매·VR 밖의 네 전략(섀넌·200일선·적립·ASAP)도 같은 방식으로 맞댔다. **7-⑮ 는 제가 N1(#238)에서 만든 회귀**다 —
섀넌·200일선·ASAP 이 체결가를 받게 됐는데 장부에 배당 칸이 없어 보유 중 받은 배당이 통째로 사라졌다.
7-⑯·⑰·⑱ 은 모의와 백테의 주수·판정 규약이 달랐던 것이다(⑱ 은 적립 모의의 룩어헤드).
고친 뒤에는 백테에만 있는 규약(예수금 이자·세금·배당 현금)을 끄면 **네 전략 모두 거래 단위로 같다.**

---

## 2. 방법 — 무엇과 무엇을 맞댔나

"같은 전략이면 같은 결과인가" 를 말(코드 읽기)이 아니라 **거래 목록**으로 본다.
`testdata/` 실데이터(SOXL·TQQQ·TECL 1,500거래일, 2020-09 ~ 2026-08)를 모든 경로에 똑같이 넣는다.
엔진은 전부 **실코드에서 떼어 와** 돌린다(재구현 금지).

### 경로 목록

| | 무한매수법 | VR |
|---|---|---|
| 운영 — 앱 | 주문표 `renderOrder` · 장부 `computeInf` | 매수·매도표 `renderVrTable` · 장부 `computeVr` · 수동 진입 `enterNextCycle` |
| 운영 — 서버 | 자동주문 `imOrders`·`imCompute` (`_im.js`) · 시세·시각 `autotrade.js` | (없음) |
| 운영 — 5년 플랜 | `imOrders`·`calcInfState` (`plan.html`) | `vrOrders`·`calcVrState`·`advancePlanVrCycle` |
| 모의 | `infSimForward` | `vrSimForward` · 자동 진입 `vrAutoAdvance` |
| 과거재생 | — | `vrReplay` |
| 백테 | `runIM` (V4.0) · `runIM50` (V5.0) | `runVR` |

| | 섀넌(역분산) | 200일선 로테 | 적립(DCA) | ASAP |
|---|---|---|---|---|
| 운영 장부 | `ivsPos` | `maLedger` | `computeDca` | `asapPos` |
| 모의·과거재생 | `ivsReplay` | `maReplay` | `_paperDca` | `_paperAsap` |
| 백테 | `runIVS` | `runMA200` | `_dcaOne` | `runASAP` |

이 넷은 신호가 시세만으로 정해져 모의가 시작일부터 통째로 다시 만든다. 백테에만 있는 규약
(예수금·국채 이자, 양도세, 배당 현금, ASAP 리저브 이자)을 원문에서 한 글자씩 끄고, 수수료를 같게 두고 맞댄다 —
그 규약 차이 자체는 결정 D9 에 따로 적었다.

### '실전 흐름' 을 기준으로 삼는다

운영 경로는 주문만 내고 체결은 거래소가 한다. 그래서 **실전을 재현**했다:
매일 **아침에 어제까지의 장부로 앱 주문표를 뽑아** 그날 봉에 체결시킨다.

    지정가 매도 : 고가 ≥ 가격 → max(시가, 가격)      LOC 매도 : 종가 ≥ 가격 → 종가
    LOC 매수   : 종가 ≤ 가격 → 종가                  MOC 매도 : 종가
    기록 순서  : 지정가(장중) → 종가 매도 → 종가 매수

이 결과를 모의·백테와 **거래 하나하나** 맞댄다. 운영 3벌(앱·서버·플랜)은 같은 장부·같은 확정 종가에서
**주문 목록**을, 장부 3벌은 이력의 **매 시점**에서 평단·보유·T·잔금·리버스 상태를 맞댄다.

---

## 3. 결과 — 수정 전 / 후

### 무한매수법

| 대조 | 수정 전 | 수정 후 |
|---|---|---|
| 운영(주문표→실제 체결) ↔ 모의, 8개 설정 | **8/8 갈림** · 최종자산 −12% ~ +20% | **8/8 거래 단위 일치** |
| 백테 ↔ 모의, 10개 설정 | 리버스 ON 3개 갈림 (1주씩) | **10/10 일치** |
| 운영 주문 3벌 (앱·서버·플랜), 6개 설정 × 1,499일 | `big` 없는 세션에서 앱≠서버 **269일** · 리버스 1일차 가격 표시 다름 | **전부 같은 주문** |
| 장부 3벌 (앱·서버·플랜), ~7,900 시점 + 경계 4종 | 서버: 복합거래 0주·배당·리버스 중 출금에서 다름 | **전부 같음** |

### VR

| 대조 | 수정 전 | 수정 후 |
|---|---|---|
| 백테 ↔ 모의 ↔ 과거재생, 5개 설정 | **인출식: 모의만 체결 1,079 vs 1,003 · 최종 35,699 vs 40,222** · 나머지는 가격 소수 4~6자리 차이 | **5/5 거래·V·Pool 일치** |
| 5년 플랜 주문 ↔ 앱 매수·매도표 | 앱 표만 한도 초과 차수 뒤를 '유효' 로 표시 | **같은 차수 목록** |
| 표 ↔ 체결 엔진 | 각자 계산 | **공용 `vrTiers` 한 곳** (옛 엔진과 무작위 30만 건 비트 동일) |

### 섀넌 · 200일선 · 적립 · ASAP

| 대조 (백테 전용 규약 끔) | 수정 전 | 수정 후 |
|---|---|---|
| 섀넌 운영 재생 ↔ 백테, 9개 설정 (1배 짝 2개 포함) | **첫 거래부터 갈림** — 운영은 사고팔기 소수점(276.465127주), 백테는 사기만 정수·팔기는 소수점(45.089주) | **9/9 거래 단위 일치** · 장부 끝 보유·현금 일치 |
| 200일선 운영 재생 ↔ 백테, 3종목 × 시작 3곳 | 이력 첫 봉부터 굴리면 운영만 하루 늦게 산다 (SOXL 백테 725주@13.74 / 운영 670주@14.88 → 6년 뒤 보유 627 / 580) · 매수 주수 식이 경계에서 1주 다름 | **9/9 일치** |
| 적립 모의 ↔ 백테, 3종목 × 매일·매주·매월 · 하락 2배 | 모의만 **오늘 종가로 배수 판정** — 배수가 다른 회차 2~42회 | **9/9 일치** |
| ASAP 모의 ↔ 백테, 3종목 | 같았다 (확인만) | **3/3 일치** · 매수 805~933건 |
| 섀넌·200일선·ASAP 가격 계열 | N1 이후 체결가인데 장부에 배당 칸이 없어 **배당 누락** | N1 이전처럼 조정종가 (7-⑮ · 근본 해결은 D8) |

---

## 4. 7차 수정 목록 (7-① ~ 7-⑱)

각 항목: **무엇이 틀렸나(값으로)** → 고친 곳 → 회귀 → 변이(되돌리면 빨간불인가).

### 7-① [HIGH] 백테 리버스 쿼터매수에서 1주가 사라진다
- 배정액을 `주수 × 주문가` 로 만들었다가 `_buy` 안에서 다시 주문가로 나눠 내림 → `250.99999999999997 → 250`.
  무작위 20만 건 중 **5.25%** 에서 1주 소실. 실데이터 SOXL 20분할 2025-04-08: 백테 112주 / 모의 113주.
- 고침: 정해진 주수를 그대로 사는 `_buyN(px, q)` — `runIM`·`runIM50` 둘 다. 금액으로 되돌리지 않는다.
- 회귀 `[105]` 리버스 ON 백테↔모의 · `[4]` 차분

### 7-② [CRITICAL] 모의·백테가 쿼터매도 체결 뒤 같은 종가에 되산다 (룩어헤드)
- 실전은 장 전에 익절 지정가·쿼터매도 LOC(별지점)·매수 LOC(별지점−$0.01)를 **한꺼번에** 건다 — 쿼터매도와
  별지점 매수는 같은 종가에서 동시에 체결될 수 없다. 모의·백테는 매도가 체결된 **뒤의 T** 로 별지점을 다시 계산했다.

      SOXL 20분할 2020-09-25 — 아침 장부: 보유 317 · 평단 14.6565 · T 9.5
        아침 별지점 14.8030 → 쿼터매도 LOC 14.8030 · 매수 LOC 14.7930 · 종가 14.9865 → 쿼터매도만 체결
        모의·백테: 쿼터매도 뒤 T 9.5→7.125 로 별지점 재계산 15.4992 → 매수 LOC 15.4892 ≥ 종가 → 같은 종가에 17주 되삼
- 익절(지정가) 체결 뒤에도 같은 방식으로 매수 배정액·가격을 다시 계산했다.
- 고침: 매수 주문을 **매도 체결 전 아침 장부로** 먼저 정한다 — 백테 공용 `_imBuyPlan` (`runIM`·`runIM50`),
  모의 `infSimForward` 의 아침 계획. 전량매도로 사이클이 끝난 날에도 아침 주문은 살아 있다(4주 미만 보유에서만 겹침).
- 회귀 `[105]` (실전 재현 ↔ 모의 ↔ 백테 거래 단위 · 2020-09-25 값 · 6년 전 구간 동시 체결 0)

### 7-③ [LOW] 모의 첫 봉에서 오늘 종가를 빌려 쓴다
- `const prevC = closeAt(i-1) || cl` — 가진 봉의 맨 첫날은 전일 종가가 없어 오늘 종가로 주문을 정했다. 백테는 5차 ⑤ 에서 막았다.
- 고침: 전일 확정 종가가 없으면 그날 주문 없음. · 회귀 `[105]` 첫 봉

### 7-④ [MEDIUM] 주문 수량이 잔금을 넘는다 / 모의·백테는 오늘 종가로 자른다
- 증권사는 주문을 받을 때 **주문가 × 수량** 을 예약하고 넘으면 거부한다. 앱·서버·플랜 주문표는 제한이 없었고,
  모의·백테는 대신 **오늘 종가** 로 잘랐다(장 마감 뒤에야 아는 값).

      TQQQ 40분할 단리 2022-03-16 — 남은 회차 1.044 · 잔금 253.76 · 별지점 매수 LOC 23.2730
        주문표: 전일종가 기준 11주 → 예약 256.00 > 잔금 (증권사 거부)   모의·백테: 잔금÷오늘종가 → 10주
- 고침: 다섯 곳 모두 `q = min(배정액 ÷ 전일 확정종가, 아침 잔금 ÷ 주문가)` · 두 번째 주문은 앞 주문 예약금을 뺀 나머지로.
- 회귀 `[105]` 2022-03-16 값 · `[106]` 주문 3벌

### 7-⑤ [HIGH · 실계좌] 서버 자동주문 장부가 4·5차 감사 이전 규칙에 멈춰 있었다
- `_im.js` `imCompute` 는 앱 `computeInf` 의 사본인데 4·5차·N1 수정이 한 번도 안 들어갔다:
  - 복합거래로 0주가 돼도 사이클이 안 끝났다 (5차 ④)
  - 배당 기록을 몰라 잔금·1회매수금이 앱보다 작았다 (N1)
  - 리버스 여부를 '마지막 기록이 리버스인가' 로 봤다 — **리버스 중 출금·배당 한 줄만 적어도 일반모드로 보고 일반 주문을 냈다**
  - 리버스 규칙이 없는 분할(30분할)에서도 리버스로 봤다
- 5년 플랜 `calcInfState` 도 배당·지원 분할이 빠져 있었다.
- 고침: 서버·플랜 장부를 `computeInf` 와 한 줄씩 같은 규칙으로. · 회귀 `[106]` 장부 3벌 ~7,900 시점 + 경계 4종

### 7-⑥ [MEDIUM · 실계좌] 서버만 큰수(주문가 상한) 기본값이 20%
- 앱·백테·플랜은 15%(`IM_BIG_DEFAULT`). `big` 을 저장하지 않은 옛 세션은 앱 화면과 실제 자동주문의 처음매수 LOC 가격이 달랐다 (1,499일 중 **269일**).
- 고침: 서버도 `IM_BIG_DEFAULT = 15` · `imBigPct()`. · 회귀 `[106]`

### 7-⑦ [MEDIUM · 실계좌] 서버가 앱과 다른 가격 계열로 수량·상한을 쟀다
- N1(#238) 로 앱은 `div=1` 체결가 계열로 바꿨는데 서버는 조정종가(`q.series`)를 그대로 썼다 — **제가 N1 에서 서버를 빠뜨린 것**.
  익절 조절(20일 상승률)은 장 마감 20분 전 실행이라 **움직이는 오늘 봉** 까지 넣어 계산했다.
- 고침: 서버도 `div=1` · 체결가 우선(없으면 앱처럼 조정 기준) · 20일 창은 확정 봉까지만. · 회귀 `[109]`

### 7-⑧ [MEDIUM] 앱 리버스 별지점·익절 조절이 미확정 오늘 봉을 쓴다
- `calcStarPoint`·`imMomNow` 가 시세 배열의 끝 5개/끝 1개를 그대로 썼다 — 장중에 주문표를 열면 오늘 현재가가 섞인다.
  (실험: 확정 5일 평균 30 인데 장중 봉 1000 이 붙으면 228)
- 고침: `settledBars` 로 확정 봉만. · 회귀 `[72]` 별지점 장중 봉 값 시험 · `[110]` 익절 조절 값 시험
  (익절 조절 쪽은 처음엔 모양 시험뿐이라 옛 코드로 되돌려도 초록이었다 — 5장 변이 시험에서 잡혀 값 시험을 넣었다)

### 7-⑨ [HIGH · 실계좌] 리버스 1일차 '무조건 매도(MOC)' 가 평단 지정가로 나갔다
- 주문표가 MOC 가격 자리에 **평단** 을 적었고, 한투 전송은 모든 주문을 지정가로 보낸다 → 평단 지정가 매도.
  리버스는 가격이 평단보다 한참 아래일 때라 **체결되지 않는다** — 무조건 매도가 조용히 빠졌다.
- 고침: MOC 가격 자리엔 참고용 전일 확정 종가. 한투 전송 목록에서 MOC 를 빼고 "증권사 앱에서 장마감 시장가로 직접" 안내. · 회귀 `[109]`

### 7-⑩ [MEDIUM · 실계좌] 서버가 통화를 `st.cur` 로 읽는다 — 앱은 그 칸을 저장하지 않는다
- 늘 비어 있어 국내 종목도 **미국 장 마감·정산 시각과 미국 주문 창** 으로 판정했다.
- 고침: 종목코드로 정한다(앱 `curOf` 와 같게). · 회귀 `[109]`

### 7-⑪ [HIGH] VR 인출식에서 Pool 이 음수가 된다
- 모의 사이클 진입(`vrStepCycle`)·수동 진입·플랜 진입이 인출금을 Pool 과 상관없이 그대로 뺐다.

      TQQQ 인출식 — 첫 사이클 인출 전 Pool 1.10 · 인출금 50
        백테·과거재생: min(50, Pool) = 1.10 → Pool 0        모의: 50 → Pool −48.90 (이후 매수 한도 0, V 식에 음수 Pool)
        6년 뒤: 모의 체결 1,079 · 최종 35,699  vs  백테·재생 1,003 · 40,222
- 고침: 공용 `vrCycleFlow(c)` — 인출은 Pool 안에서만. V 식의 인출 항은 규칙대로 전액(세 경로 같음). · 회귀 `[107]`·`[109]`

### 7-⑫ [MEDIUM] VR 매수표가 한도를 넘긴 차수 뒤를 '유효' 로 보였다
- 표는 넘친 차수만 건너뛰고 다음 차수를 계속 봤다 — 뒤 차수는 값이 싸서 남은 한도에 다시 들어갔다.
  체결 엔진은 첫 초과 차수에서 멈춘다 → **표에 있는데 모의·백테에선 절대 체결되지 않는 주문**.
- 고침: 공용 `vrTiers()` — 주문표·체결 엔진이 같은 차수 목록을 받는다. 옛 엔진과 **무작위 30만 건 비트 동일** 확인. · 회귀 `[108]`

### 7-⑬ [LOW] VR 기록 자릿수가 경로마다 달랐다
- 모의는 첫 매수 잔돈을 센트로(1.103897 → 1.10), 가격을 소수 4자리로, V 를 4자리로 저장 — 과거재생은 6자리.
  V 식(V + Pool/G)을 타고 번져 6년 뒤 V 0.07 차이.
- 고침: 모의·수동 진입·플랜 모두 6자리. · 회귀 `[107]`

### 7-⑭ [MEDIUM] VR 수동 진입의 평가금 E 가 장중 현재가
- 실력공식 `V + Pool/G + (E−V)/(2√G)` 의 E 를 `vrEval`(마지막 봉 — 장중이면 움직이는 값)로 쟀다. 모의·백테는 진입일 확정 종가.
- 고침: 진입일(휴장이면 다음 거래일) 확정 종가 × 보유 · 조기 진입이면 마지막 확정 종가 · 진입일 종가가 아직 없으면 실력공식은 진입을 막는다.
  계산은 순수 함수 `vrEntryEval` 로 떼어 값으로 시험한다 (처음엔 모양 시험뿐이라 변이 시험에서 살아남았다).
- 회귀 `[109]` 배선 · `[110]` 값 (휴장 진입일 → 다음 거래일 110 · 조기 진입 → 마지막 확정 120 · 수동 입력 우선)

### 7-⑮ [HIGH · 제가 만든 회귀] 섀넌·200일선·ASAP 에서 배당이 사라졌다 (N1 부작용)
- N1(#238)은 공용 로더 `_fetchDailyRaw` 를 체결가 계열로 바꿨다. 무매·VR 에는 배당 기록(`'배당'`·`type:'div'`)을 같이
  만들었지만, 같은 로더를 쓰는 **섀넌·200일선·ASAP 은 장부에 배당 칸이 없어** 보유 중 받은 배당이 모의·재생·분석에서 통째로 빠졌다.
  실데이터(배포 API 체결가·조정가·배당 · 2015-01 ~ 2026-09 · 1만$) — 백테(체결가 + 배당 현금) 대비 모의 최종자산:

      섀넌 (s0 55·N 40·밴드 15)   TQQQ  체결가(N1 상태) −2.4% · 조정가 +2.0%
                                  SOXL          −3.6%      ·        +1.7%
                                  TECL          −8.1%      ·        −0.6%
      200일선 (200선·현금 대피)   TQQQ          −4.1%      ·        +7.6%
                                  SOXL          −5.3%      ·        −4.7%
                                  TECL          −9.4%      ·        +9.9%

- 고침: 세 로더만 조정 뷰 `adjDaily(q)` 를 받는다 — **N1 이전 상태로 되돌림**. 공용 캐시 객체는 건드리지 않아 무매·VR 은 체결가 그대로.
  `priceBasis:'adjusted'` 라 배당을 또 얹지 않는다(이중계상 방지). 섀넌·200일선 화면 설명문에도 '조정종가로 굴린다' 고 적었다.
- **남는 것 → D8**: 조정가는 신호 계열이 백테(체결가)와 달라진다. 200일선 신호(보유/현금)가 2011 ~ 3,952거래일 중
  **TQQQ 4일 · SOXL 1일 · TECL 13일** 갈리고, 그 며칠이 휩소 구간이면 위처럼 ±10% 까지 벌어진다(방향은 종목마다 다름).
  섀넌 목표비중 차이는 평균 0.02%p · 최대 3.4%p. 근본 해결은 세 전략도 체결가 + 배당 기록으로 가는 것 —
  장부·화면에 배당 칸을 새로 만드는 일이라 결정으로 올린다.
- 회귀 `[111]` (조정 뷰 값 · 배당 이중계상 없음 · 공용 캐시 불변 · 세 로더만 조정 · 무매·VR 로더는 체결가)

### 7-⑯ [MEDIUM] 섀넌 주수 규약이 운영·백테가 달랐다
- 운영 재생 `ivsReplay` 는 사고팔기 둘 다 **소수점**, 백테 `runIVS` 는 **사기만 정수 · 팔기는 소수점**이었다
  (백테 규약표에는 '정수' 라고 적혀 있었다). 첫 매도부터 보유가 소수점이 되고 두 경로가 갈렸다.

      TQQQ s0 55 · N 40 · 밴드 15   첫 매수 2021-09-03 — 백테 276주 / 운영 276.465127주
                                   첫 매도 2021-12-28 — 백테 45.089주 / 운영(소수점) 45.089주 · 고친 뒤 둘 다 45주

- 고침: 둘 다 정수. 매수는 `imBuyQty`(배정금 ÷ (가격×(1+수수료)) 내림 — 무매와 같은 헬퍼), 매도는 `isq`(내림 · 보유분 초과 방지).
  총자산을 더하는 순서까지 백테와 같게 해 부동소수 끝자리도 같다(지금 데이터에선 안 걸리지만 경계에서 한 건이 갈릴 수 있다).
- 백테 영향: 대부분 ±1% 안이지만 **TECL 역분산 −10.3% · SOXL 고정 5:5 −9.1%** (testdata · 비용 끔) —
  한 주 차이가 밴드 판정 날짜를 바꾸고 3배 레버리지에서 경로가 갈라진다. 방향은 설정마다 다르다(6장 표).
- 회귀 `[112]` (9개 설정 거래 단위 · 주수 전부 정수 · 운영 장부 끝값 = 백테) · `[80]` 조건 강화(운영 ⊆ 백테 · 겹침 ≥90%)

### 7-⑰ [LOW] 200일선 운영 재생의 매수 주수 식 · 이력 첫 봉
- 매수 주수: 운영 `(현금 − 현금×수수료) ÷ 가격` · 백테 `현금 ÷ (1+수수료) ÷ 가격` — 경계에서 1주 다르다
  (가격 99.7503 · 현금 10,000 → 운영 99주 / 백테 100주).
- 이력 첫 봉: 운영은 '전일 신호 없음' 으로 건너뛰고, 백테는 워밍업을 보유로 보고 첫날 산다(`computeMa` 도 워밍업은 보유라고 적혀 있다).
  상장 첫날부터 재생하면 SOXL 백테 725주@13.74 / 운영 670주@14.88(하루 늦게) → 6년 뒤 보유 627 / 580.
- 함께: 백테 `runMA200` 이 1주도 못 사는 돈일 때 0주 '매수' 를 보유 신호가 이어지는 동안 **매일 1건씩 거래로 셌다** — 1주 이상일 때만 거래.
- 회귀 `[113]` (3종목 × 시작 3곳 거래 단위 · 장부 끝값 · 경계 픽스처 · 0주 거래 0건)

### 7-⑱ [MEDIUM · 룩어헤드] 적립 모의가 오늘 종가로 하락 배수를 정했다
- `_paperDca` 가 `오늘 종가 ≤ 오늘 200일선` 을 보고 그 종가에 배수로 샀다 — 장 마감 뒤에야 아는 값으로 그날 주문 금액을 정한 것.
  백테 `_dcaOne` 은 1차 감사(#210)에서 전일 값으로 고쳤는데 모의가 남아 있었다.
- 영향(testdata · 월 10$ · 하락 2배): 배수가 다른 회차 매일 28~42회 · 매주 6~8회 · 매월 2~3회 — 수익률 −0.7 ~ +5.3%p.
- 고침: 전일 종가 ≤ 전일 200일선. · 회귀 `[114]` (3종목 × 매일·매주·매월 거래 단위 · 처음 200선 아래로 온 날은 1배 · 이튿날부터 2배)

### 함께 확인한 것 — ASAP
모의 `_paperAsap` ↔ 백테 `runASAP` 는 리저브 이자·배당·비용을 끄면 3종목 매수 805~933건이 날짜·금액까지 같았다(고칠 것 없음).
앞으로 갈리지 않게 회귀 `[115]` 로 묶었다 — 지표를 오늘 종가까지로 바꾸는 변이를 넣으면 빨간불이 된다.

### 함께 고친 것 — 코드와 정반대였던 VR 엔진 주석
엔진 위 주석이 "공식 = V 복귀 · 1주 사다리 = 커스텀" 이라고 적혀 있었는데 코드는 #236 이후 사다리였다.
감사가 주석을 근거로 하면 또 헛짚는다 — 실제 동작과 원문 미확인 사실을 그대로 적었다 (본문 D4).

### 함께 고친 것 — 시험이 코드를 재구현하던 자리
`[100]` 은 플랜 장부를 시험할 때 `KIND_T`·매수/매도 판정을 **시험 안에서 따로 지어 넣었다** — 플랜 코드가 틀려도
시험 자기 사본으로 통과했다. 플랜의 실제 정의를 떼어 넣도록 바꿨다.

---

## 5. 변이 시험 — 되돌리면 빨간불인가

'시험이 있다' 가 아니라 **'옛 코드로 되돌리면 시험이 실제로 빨개지는가'** 를 봤다.
저장소를 통째로 복사해 한 곳만 옛 코드로 되돌리고 회귀 전체를 돌린다 — 35종.

| 수정 | 되돌린 것 | 결과 | 잡은 시험 |
|---|---|---|---|
| 7-① | 백테 리버스 쿼터매수 주수→금액→주수 왕복 | 🔴 | `[105]` 백테↔모의 |
| 7-② | 백테 `runIM` 매도 뒤 장부로 매수 재계산 | 🔴 | `[105]` |
| 7-② | 모의 매도 뒤 매수 재계산 (간이 되돌림) | 🔴 4건 | `[105]` |
| 7-② | 모의 매도 뒤 매수 재계산 (처음매수 분기까지 옛 코드 그대로) | 🔴 10건 | `[105]` 2020-09-25 · 실전 재현 |
| 7-③ | 모의 첫 봉 `전일종가 \|\| 오늘종가` | 🔴 | `[105]` 첫 봉 |
| 7-④ | 앱 주문표 잔금 한도 없음 | 🔴 3건 | `[105]` 2022-03-16 · `[106]` |
| 7-④ | 모의 잔금 한도를 오늘 종가로 | 🔴 4건 | `[105]` |
| 7-⑤ | 서버 사이클 종료를 매도 전용 기록으로만 | 🔴 | `[106]` 경계 |
| 7-⑤ | 서버 비매매 기록이 리버스를 풀어 버림 | 🔴 | `[106]` 경계 |
| 7-⑤ | 서버 배당 무시 | 🔴 4건 | `[106]` 장부 3벌 |
| 7-⑥ | 서버 큰수 기본값 20 | 🔴 | `[106]` 주문 3벌 |
| 7-⑦ | 서버 시세 `div=1` 빼기 | 🔴 | `[109]` |
| 7-⑧ | 리버스 별지점 미확정 봉 포함 | 🔴 | `[72]` |
| 7-⑧ | 익절 조절 미확정 봉 포함 | 🟢 **생존** → 값 시험 `[110]` 추가 → 🔴 | `[110]` 20% vs 98% |
| 7-⑨ | 한투 전송에 MOC 포함 | 🔴 | `[109]` |
| 7-⑨ | 리버스 1일차 MOC 가격 = 평단 | 🔴 | `[106]` 주문 3벌 · `[109]` |
| 7-⑩ | 서버 통화 = `st.cur` | 🔴 | `[109]` |
| 7-⑪ | VR 인출 Pool 안 봄 | 🔴 5건 | `[107]` 백테↔모의 · `[109]` |
| 7-⑫ | `vrTiers` 넘친 차수만 건너뜀 | 🔴 | `[108]` |
| 7-⑬ | 모의 첫 매수 잔돈 센트 반올림 | 🔴 5건 | `[107]` V·Pool |
| 7-⑭ | 수동 진입 평가금 = `vrEval` | 🟢 **생존** → `vrEntryEval` 분리 · `[110]` → 🔴 | `[109]` 배선 |
| 7-⑭ | 평가금 함수가 진입일 봉 무시 | 🔴 | `[110]` 110 vs 120 |
| 7-⑮ | `adjDaily` 가 받은 그대로(체결가) | 🔴 4건 | `[111]` |
| 7-⑮ | 200일선 로더가 체결가 직통 | 🔴 | `[111]` |
| 7-⑮ | 섀넌 1배 짝이 체결가 직통 | 🔴 | `[111]` |
| 7-⑯ | 운영 섀넌 사고팔기 소수점 (옛 코드) | 🔴 30건 | `[112]` · `[80]` |
| 7-⑯ | 운영 섀넌 매도만 소수점 | 🔴 30건 | `[112]` · `[80]` |
| 7-⑯ | 백테 섀넌 매도 소수점 (옛 코드) | 🔴 30건 | `[112]` · `[80]` |
| 7-⑯ | 백테 섀넌 1배 짝 매도만 소수점 | 🔴 9건 | `[112]` 짝=1배 |
| 7-⑯ | 운영 섀넌 총자산 더하는 순서 (옛 코드) | 🔴 | `[112]` 모양 시험 — **값으로는 이 데이터에서 안 갈린다** |
| 7-⑰ | 운영 200일선 옛 주수 식 | 🔴 | `[113]` 경계 99 vs 100 |
| 7-⑰ | 운영 200일선 이력 첫 봉 건너뜀 | 🔴 7건 | `[113]` |
| 7-⑰ | 백테 0주 매수를 거래로 셈 | 🔴 | `[113]` 거래 5 vs 0 |
| 7-⑱ | 적립 모의 오늘 종가로 배수 판정 | 🔴 10건 | `[114]` |
| (확인) | ASAP 모의 지표를 오늘 종가까지 | 🔴 4건 | `[115]` · `[71]` |

**생존 2종이 말해 주는 것** — 둘 다 '코드 모양' 만 보는 시험이었다. 모양 시험은 되돌리는 방식이 조금만 달라도 못 잡는다.
그래서 둘 다 순수 함수로 떼어 **값** 으로 묶었다. 7-⑯ '더하는 순서' 1종은 여전히 모양으로만 잡힌다(값으로는 이 데이터에서 차이가 안 난다) — 그렇게 적어 둔다.
시험 쪽 결함도 하나 나왔다 — `[113]` 의 기록 훅이 새 코드 모양에만 걸려 있어 옛 코드로 되돌리면 시험이 '빨개지는' 게 아니라 **죽었다**. 훅을 두 모양 모두에 걸리게 고친 뒤 값 시험이 잡는 것을 확인했다.

---

## 6. 수정 전후 백테 수치 (사용자 결과가 바뀌는 곳)

같은 데이터 · 원금 1만$ · 비용 끔 · 익절=고가. 차이는 거의 전부 7-② 에서 온다 — 오르는 장에선 되사기가
수익을 부풀리고, 내리는 장에선 손실을 키워 **설정마다 방향이 다르다.**

| 설정 | 수정 전 | 수정 후 | 변화 | CAGR 전 → 후 |
|---|---|---|---|---|
| SOXL 20분할 20% 복리 | 128,281 | 109,802 | −14.4% | 53.4% → 49.5% |
| SOXL 40분할 20% 복리 | 55,969 | 63,637 | +13.7% | 33.5% → 36.4% |
| SOXL 20분할 20% 단리 | 36,675 | 34,983 | −4.6% | 24.3% → 23.4% |
| TQQQ 20분할 15% 복리 | 27,607 | 28,460 | +3.1% | 18.6% → 19.2% |
| TQQQ 40분할 15% 복리 | 24,742 | 28,741 | +16.2% | 16.4% → 19.4% |
| TECL 20분할 20% 복리 | 48,966 | 40,768 | −16.7% | 30.5% → 26.6% |
| SOXL 20분할 20% 복리 리버스 | 84,636 | 92,183 | +8.9% | 43.1% → 45.1% |
| TQQQ 40분할 15% 복리 리버스 | 22,205 | 22,100 | −0.5% | 14.3% → 14.2% |

**7-⑯ 섀넌 (testdata · 1만$ · 앱 기본 s0 55 · N 40 · 밴드 15)** — 한 주 차이가 밴드 판정 날을 바꿔 경로가 갈린다. 방향은 설정마다 다르다.

| 설정 | 비용 | 수정 전 | 수정 후 | 변화 | 거래 |
|---|---|---|---|---|---|
| SOXL 역분산 | 끔 | 80,459 | 80,421 | −0.05% | 45 → 45 |
| TQQQ 역분산 | 끔 | 56,306 | 56,188 | −0.21% | 39 → 39 |
| TECL 역분산 | 끔 | 66,435 | 59,600 | **−10.29%** | 38 → 38 |
| SOXL 고정 5:5 | 끔 | 72,593 | 65,967 | **−9.13%** | 15 → 17 |
| TQQQ 역분산 s0 45·N 60·밴드 10 | 끔 | 33,400 | 34,277 | +2.63% | 67 → 68 |
| SOXL 역분산 | 켬 | 60,661 | 60,661 | 0.00% | 46 → 46 |
| TQQQ 역분산 | 켬 | 48,042 | 48,365 | +0.67% | 41 → 41 |
| TECL 역분산 | 켬 | 52,171 | 52,088 | −0.16% | 38 → 38 |
| SOXL 고정 5:5 | 켬 | 67,930 | 61,920 | **−8.85%** | 17 → 18 |

비용을 켜면 연말 세금 강제매도가 **소수점 주수**로 팔아 보유가 다시 소수점이 된다(TQQQ 끝 보유 496.718주) — 전 백테 공통 규약이라 이번엔 안 건드렸다(D10).

**7-⑰ 200일선 백테** — 원금이 1주 값보다 클 때는 결과가 한 푼도 안 바뀐다(거래 수만 바로잡힘).
**7-⑮·⑱** 은 백테가 아니라 모의·과거재생 쪽 수치가 바뀐다(4장 7-⑮·7-⑱).

**기존 모의 세션**은 이미 쌓인 기록이 옛 규칙으로 만들어져 있다. 새로 굴리면 새 규칙으로 이어진다 —
과거 구간까지 맞추려면 모의 세션을 시작일부터 다시 만들어야 한다(모의 성과 창에서 재생성).
섀넌·200일선·적립·ASAP 모의는 새 봉이 들어오는 날 **시작일부터 통째로 다시 만들므로** 저절로 새 규칙이 된다.

---

## 7. 남은 것 — 결정이 필요한 것 (7차 기준, 코드 안 바꿈)

| # | 내용 | 왜 결정이 필요한가 |
|---|---|---|
| **D1** (N3) | **익절 체결 모델** — 모의·백테 기본은 '장중 고가 터치'. 서버 자동주문은 마감 20분 전(15:40 ET)에 지정가를 넣으므로 실제로는 마지막 20분만 살아 있다. 고가 vs 종가 차이 −7.3% ~ +3.4% (SOXL·TQQQ·TECL 20분할) | 수동으로 장 전에 거는 사람은 '고가' 가 맞고, 자동주문은 '종가' 에 가깝다 — 운영 방식에 따라 답이 다르다. 자동주문 세션은 모의를 '종가' 로 돌릴지, 익절 지정가만 아침에 따로 걸지 정해야 한다 |
| **D2** | **한투는 해외주식 지정가만 받는다** — 자동주문은 LOC 를 15:40 지정가로 흉내 낸다(마지막 20분 안 아무 때나 체결). 앱 모의 장부는 LOC(종가) 모델이고, 한투 체결 내역을 장부로 가져오지 않는다 → 모의+자동주문 세션은 한투 모의계좌와 앱 장부가 서서히 벌어질 수 있다 | 체결 내역 동기화를 만들지, 차이를 표시만 할지 |
| **D3** | **하방 LOC(`rowsOn`)** — 운영 3벌은 주문을 내지만 모의·백테는 시뮬레이션하지 않는다. 체결됐을 때 T 규칙도 원문에 없다(커스텀) | 모의·백테에 넣을지, '모의·백테 미반영' 표시로 둘지 |
| **D4** | **VR 체결 모델 원문** — 5차 감사 'V 복귀'(#235) ↔ #236 '사이클 고정 20차 1주 사다리'. 지금은 사다리 | 원문 확인 전까지 어느 쪽도 '공식' 이라 단정 못 한다 |
| **D5** | **VR 사다리 가격 호가 단위** — 엔진은 `up/(B−k+1)` 같은 소수 가격에 체결시키지만 실제 지정가는 센트 단위 | 매도는 올림·매수는 내림으로 반올림해 체결 판정할지 |
| **D6** (N5) | **V 갱신식 3벌** (`computeNextV` · `vrReplay` · `runVR`, + 플랜) | 7차 교차 대조가 세 곳 결과가 같음을 매번 확인한다. 한 함수로 합칠지는 선택 |
| D7 | 5차 ⑦ 무매 `target` 변경 시 공식/커스텀 표시 · 5차 ⑧ 같은 날 양방향 체결 카운트 | 미착수 |
| **D8** (7-⑮) | **섀넌·200일선·ASAP 도 체결가 + 배당 기록으로** — 지금은 N1 이전처럼 조정종가. 조정가는 배당을 가격에 품지만 신호 계열이 백테(체결가)와 달라 200일선 신호가 15년에 1~13일 갈린다. 체결가만 쓰면 신호는 같아지지만 배당이 빠진다(모의 −2.4 ~ −9.4%) | 둘 다 온전하려면 세 장부(`ivsPos`·`maLedger`·`asapPos`)와 기록 화면에 배당 칸을 새로 만들어야 한다 — 기능 추가라 물어본다 |
| **D9** | **모의에 없는 백테 규약** — 섀넌·200일선: 예수금·국채 이자, 양도세 · 적립·ASAP: 매수 수수료 · ASAP: 리저브(SGOV) 이자 · 200일선: 단기선 길이(백테 20/30/50 선택 · 앱 50 고정) | 모의를 백테에 맞출지, 화면에 '모의는 후하게 나온다' 로 둘지(지금은 후자 — 각 탭 설명문에 적혀 있다) |
| **D10** | **세금 강제매도가 소수점 주수** — 정수 주수 전략(무매·VR·섀넌·200일선)도 연말 세금 현금이 모자라 파는 몫은 소수점이다 | 올림 정수로 팔지(현금이 조금 남는다), 지금처럼 둘지 |

---

## 8. GPT 에 확인 요청 (7차 갱신)

1. **[D4] VR 예약주문표 원문** — 밴드를 벗어나면 (a) 1주씩 밴드 경계에 붙이는 사다리인가, (b) 한 번에 V 까지 되돌리는 수량인가?
   사이클 첫날 표를 고정하는가(현재 구현: 시작 보유수량 B 로 고정)? 원문 문장 인용 부탁.
2. **[D1] 무매 익절 지정가** — 원문은 장 시작 전(또는 프리장)부터 걸어 두는가? 그렇다면 자동주문을 마감 20분 전에 넣는 것 자체가
   원문과 다른 운영이다.
3. **[7-②] 같은 날 익절 체결 후 매수** — 원문 흐름도 '아침에 매수·매도를 한꺼번에 걸고 다음 날 장부로 다시 계산' 이 맞는가?
   (7차 수정은 이 전제다. 장중에 익절이 나면 그날 매수 LOC 를 새 장부로 다시 거는 운영이 원문이라면 7-② 는 되돌려야 한다.)
4. **VR 실력공식** `V + Pool/G + (E−V)/(2√G) ± add` 의 원전 출처.
5. **VR 인출식** — Pool 이 인출금보다 적을 때 원문 처리 (7-⑪ 은 'Pool 안에서만' · V 식 인출 항은 전액).
6. **무매 리버스 매수가** — 별지점 −$0.01 의 원문 근거.
7. **[D8] 200일선(LRS) 신호 계열** — 원문(Gayed LRS 계열)은 기초지수를 **가격지수** 로 보는가, **총수익지수(배당 포함)** 로 보는가?
   답에 따라 앱·백테의 신호 계열(지금 앱 조정가 · 백테 체결가)을 어느 쪽으로 모을지 정해진다.
8. **[7-⑯] 섀넌(역분산) 리밸런싱 주수** — 원문 규칙이 소수점 주수(비중 정확히 맞춤)를 전제하는가? 정수 주수로 끊는 게 원문과 다른 운영인가?

---

## 부록 A — 자체 점검 1차 (1부) 왜 계속 나오는가 (검증 방식의 구멍)

### 1-1. 회귀가 '코드의 현재 동작'을 정답으로 굳힌다

가장 큰 원인이다. 버그를 고칠 때 시험을 같이 넣는데, 그 시험은 **내가 이해한 규칙**을
적는다. 이해가 틀리면 시험이 **틀린 동작을 정답으로 고정**한다. 실제 사례:

| 회귀 | 무엇을 정답으로 굳혔나 | 실제 규칙 |
|---|---|---|
| `[32]` | `Math.floor((B.amt/2)/cl)` — **오늘 종가**로 주문수량 산정 | 주문 전 아는 가격(전일 종가) |
| `[72]` | 시험 데이터를 `Math.max(1, Math.floor(qty/10))` 로 생성 | 내림 그대로, 0주면 매도 없음 |
| `[69]` | `up/q` · `dn/q` 1주 사다리를 정상으로 검사 | ~~공식 VR 은 평가금을 V로 되돌린다~~ — **7차 정정: #236 에서 사다리로 되돌림 · 원문 미확인 (본문 D4)** |

`[32]`·`[72]`는 **시험이 버그를 보호하고 있었다.** 그래서 회귀를 아무리 돌려도
안 나온다. 외부 감사가 원문과 대조해야만 나오는 종류다.

`[69]` 는 더 나빴다. **"앱 사다리 == 백테 사다리"** 만 검사해서, 둘이 **같은 방식으로
함께 틀려 있어도 통과**했다. 실제로 양쪽 다 밴드를 넘어도 1주씩만 움직여
평가금이 V로 돌아오지 않았다 — 공식 VR 5.0 이 아닌 다른 전략이었다.
지금은 원문 규칙에서 손으로 계산한 골든값으로 대조한다.

### 1-2. PASS 개수는 커버리지가 아니다

1526개는 "1526개 주장이 참"이라는 뜻이지 "규칙을 다 덮었다"는 뜻이 아니다.
시험이 **버그 신고에서 자라났고 원문에서 자라나지 않았다.** 규칙 → 경로 매핑표가
없어서, 아무도 신고하지 않은 경로는 계속 빈칸으로 남았다.

### 1-3. 같은 전략이 여러 갈래로 구현돼 있다

무매 7갈래 · VR 7갈래다. 나는 **2~3곳을 고치고 "고쳤다"고 보고**했다.
이게 "전에 제대로 구현돼 있다고 했잖아"의 정체다 — 내가 본 갈래는 맞았고,
안 본 갈래가 틀려 있었다.

### 1-4. 원문이 없다

나는 원문 PDF를 갖고 있지 않다. 코드 주석과 코드가 **같이** 틀려 있으면 못 잡는다.
실제로 `revGap` 기본값(2.5 vs 0), 리버스 매도 1주 강제가 그랬다.

### 결론 — 검증 방식을 바꿔야 한다

- 시험 데이터를 **구현 함수로 만들지 않는다** (손으로 적은 기대값만)
- 규칙마다 **경로 × 규칙 대조표**를 만들고 빈칸을 남기지 않는다
- 새 규칙은 **원문 인용 → 시험 → 구현** 순서로 간다

---

## 부록 B — 자체 점검 1차 (2부) 경로 × 규칙 대조표

> 7차에서 이 표는 **'글자 대조'** 였다는 게 드러났다 — 표에 O 가 다 차 있어도 경로끼리 거래가 달랐다(7-②).
> 7차부터는 거래 단위 대조(본문 2~3장)를 기준으로 삼는다.

`O` = 해당 표현이 그 함수 안에 있음 · `·` = 없음

### 무한매수법 (7갈래)

A=운영 주문표 `renderOrder` · B=운영 장부 `computeInf` · C=모의체결 `infSimForward`
D=백테 V4.0 `runIM` · E=백테 V5.0 `runIM50` · F=백테 V2.2 `runIM22` · G=백테 V3.0 `runIM30`

| 규칙 | A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|---|
| 리버스 매도분모 sellDiv | O | · | O | O | O | · | · |
| 리버스 매도 1주 강제 (있으면 버그) | · | · | · | · | · | · | · |
| 리버스 매도 내림 그대로 | O | · | O | O | O | · | · |
| 리버스 T ×0.9/×0.95 | · | · | · | O | O | · | · |
| 리버스 매수 T+(div−T)×0.25 | · | · | · | O | O | · | · |
| 리버스 매수가 별지점−0.01 | O | · | O | O | O | · | · |
| 리버스 5일평균(오늘 제외) | · | · | O | O | O | · | · |
| 복귀 조건 종가>평단×exitMul | O | · | O | O | O | · | · |
| 큰수 상한 `imBigPct` | O | · | O | O | O | · | · |
| 주문수량 `imBuyQty` | O | · | O | O | O | · | · |
| 익절 체결가 max(익절가,시가) | · | · | O | O | O | · | · |
| 세무원가 lotBuy/lotSell | · | · | · | O | O | O | O |
| 마지막해 settleToStable | · | · | · | O | O | O | O |

**읽는 법**: B(운영 장부)는 기록을 되감는 함수라 주문 규칙이 없는 게 정상이다.
F·G(V2.2·V3.0)는 구버전 엔진이라 리버스가 없는 게 정상이다.
**A 열의 `·` 중 "리버스 T", "리버스 5일평균"은 운영 주문표가 T를 직접 굴리지 않고
`computeInf` 결과를 받아 쓰기 때문**이라 정상이다.

### VR (7갈래)

A=운영 주문표 `renderVrTable` · B=운영 장부 `computeVr` · C=**공용 엔진 `vrOrderPlan`**
D=모의체결 `vrSimForward` · E=과거재생 `vrReplay` · F=자동진입 `vrAutoAdvance` · G=백테 `runVR`

| 규칙 | A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|---|
| 사이클 14일 `vrNextDue` | · | · | · | O | O | · | O |
| 밴드 up/dn = V×(1±band) | · | · | O | · | O | · | O |
| 매도차수 up/보유 | · | · | O | · | · | · | · |
| 매수차수 dn/보유 | · | · | O | · | · | · | · |
| **공용 엔진 `vrOrderPlan` 호출** | · | · | O | O | O | · | O |
| 고가·저가 체결 | · | · | O | O | · | · | O |
| 사이클 한도 `budgetRemaining` | · | · | O | O | O | · | O |
| V 갱신 기본식 V+Pool/G | · | · | · | · | **O** | · | **O** |
| V 갱신 실력식 (E−V)/(2√G) | · | · | · | · | **O** | · | **O** |
| 이어받기 정수주+잔돈 | · | · | · | · | O | · | O |
| 수수료 FEE | · | · | O | O | O | · | O |
| **배당 `divCash`** (→ N1) | · | · | · | · | · | · | **O** |
| 확정 마감일 `simCutoff`/`_lastSettled` | · | · | · | O | O | O | · |

**바뀐 것** — 매도·매수 차수가 이제 C 한 곳뿐이다(전에는 C·G 양쪽에 각자 있었다 → N4 해결).
D·E·G 가 전부 공용 엔진을 부른다. 확정 마감일은 D·E·F 셋 다 쓴다(G는 백테라 '오늘'이 없다 → 정상).

**아직 굵은 것** — V 갱신식이 E·G 에 각자 남아 있다(N5). 배당은 G 에만 있다(N1).

굵은 `O` = **중복 구현**(같은 규칙이 두 곳 이상에 각자 적혀 있음)

---

## 부록 C — 자체 점검 1차 (3부) N1~N6

### N1 [CRITICAL · 수정 완료 v3.83.0] 앱과 백테가 **서로 다른 주가 시계열** 위에서 돌았다

가장 큰 갈래다. 감사 ⑦이 지적한 것의 뿌리이고, 내가 처음에 "VR 배당이 없다"로 잘못 좁혔던 것이다.

**백테** (2·3차 감사에서 고친 상태)
- `M` = **실제 체결가** (분할만 소급 · 배당 미반영), `ADJ` = 조정종가, `DIVMAP` = 배당 이벤트
- 배당은 `divCash(tkr,d,shares,costOn)` 로 **보유 주수만큼 현금 지급**
- `backtest.html` 의 `divCash(` 호출 **14곳** (runIM · runVR · runStdev · runMA200 · …)

**앱 (운영 주문표 · 모의체결 · 과거재생)**
- `_fetchDailyRaw` → `/api/quote?symbol=X&range=max` — **`div=1` 이 없다**
- `functions/api/quote.js:228` → `ohlcTrade: wantDiv ? ohlcTrade : null`
  → `div=1` 없이 부르면 **`ohlcTrade` 는 null 이고 `ohlc` 는 조정 기준**
  (`quote.js:203` — "O/H/L을 종가와 같은 조정 기준으로 통일: f = adjClose/rawClose")
- `index.html` 의 `divCash(` 호출 **0곳**
- 배당은 `divOf(sym)` 으로 따로 받되 **`divmode==='cash'` 인 세션에서만** 쓴다
  (`index.html:6530` 주석: *"재투자면 가격 경로에 이미 반영돼 있어서 또 더하면 이중계상"*)

**즉 두 설계가 다르다.**

| | 주가 시계열 | 배당 |
|---|---|---|
| 앱 (운영·모의·재생) | **조정종가(총수익)** — 배당이 가격에 녹아 있음 | 재투자면 안 줌(이미 내재) · 현금수령이면 따로 뺌 |
| 백테 | **실제 체결가** | 이벤트로 현금 지급 |

**왜 결과가 갈리나** — 무매는 평단·별지점·1회매수금·주문수량이 전부 주가로 정해지고,
VR 사다리 차수는 `up ÷ 보유주수` 라 주식수에 직접 걸린다. 가격 시계열이 다르면
**같은 규칙이라도 다른 주식수·다른 평단**이 되고, 그 뒤 경로가 통째로 갈라진다.
`backtest.html` 이 같은 문제를 고칠 때 실측한 차이는 **QQQ 2022-01-03 체결가 100.07 vs 조정 98.47 (1.6%)** 였다.

`quote.js:195` 주석이 이미 이렇게 적고 있다 —
*"adjclose 를 체결가로 쓰면 '배당까지 얹힌 가격에 그 주수를 샀다'가 돼"*.
**백테는 그래서 고쳤고, 앱은 아직 안 고쳤다.**

**고친 것 (v3.83.0)** — 체결가 전환과 배당 이벤트는 **같이** 해야 한다.
체결가로만 바꾸면 배당이 가격에서 빠지면서 재투자 세션이 그 돈을 통째로 잃는다.

1. **로더** — `/api/quote?…&range=max&div=1` 로 받는다. 옮기는 규칙은
   순수 함수 `quoteToDaily(SYM, j)` 로 떼어 정규식이 아니라 **값으로** 시험한다.
   - 체결가 계열이 오면 `days`·`ohlc` 를 그걸로 내보낸다 — 부르는 쪽이 수십 곳이라
     여기서 한 번 갈아 끼우면 전 경로가 같이 바뀐다
   - 조정계열은 총수익 분석용으로 `daysAdj`·`ohlcAdj` 에 남긴다
   - 체결가 계열이 없으면(`priceBasis!=='trade'`) 예전처럼 조정 기준으로 돈다 —
     없는 걸 지어내지 않고, `trade` 라고 거짓말도 하지 않는다
   - 국내 종목 현재가는 그대로 네이버 것을 쓴다(`quote.js` 가 그렇게 돌려준다).
     야후가 안 되면 네이버 일봉을 체결가로 쓴다 — 걱정하던 부분을 회귀가 지킨다

2. **가격 역할 헬퍼** — `isTradeBasis(Q)` · `divPerShareQ(Q,d)` · `divCashQ(Q,d,shares,taxOn)`.
   백테 `divPerShare`/`divCash` 와 **같은 식 · 같은 세율(`DIV_TAXRATE=0.154`)** 이고,
   회귀가 두 파일의 값을 직접 맞춰 본다. 조정 기준일 땐 0 을 낸다 — 이중계상을 막는 한 줄.

3. **무매 장부에 배당 칸을 새로 만들었다** — 무매엔 배당이라는 개념이 아예 없었다.
   `'배당'` 기록(`amt`)을 더해 `computeInf` 가 `divTotal` 로 세고 잔금에 더한다.
   T·평단·보유·리버스 상태는 건드리지 않는다(출금과 같은 성격). 단리 인출 판정
   (`사이클 종료 잔액 > 원금`)에도 들어간다 — 계좌에 실제로 들어온 돈이다.

4. **VR 장부는 `type:'div'` 를 원래 읽었는데 만들어 주는 코드가 없었다** (`grep` 0건).
   모의체결·과거재생이 배당락일마다 스스로 적는다.

5. **생성 위치** — 모의체결(`infSimForward`·`vrSimForward`)·과거재생(`vrReplay`).
   그날 사다리·주문보다 **먼저** 넣는다 — 배당은 전날 종가 시점의 보유 주수로 받고,
   그 현금이 그날 매수 여력이 된다. 백테(`runIM`·`runVR`)도 같은 순서다.

6. **`divmode==='cash'` 세션은 안 적는다** — 계좌 밖으로 나가는 돈이라
   분배금 카드(`sessDivCash`)가 따로 센다. 두 군데서 세면 또 갈린다.

> **7차 정정 (7-⑮)** — N1 은 공용 로더를 바꿨기 때문에 **섀넌·200일선·ASAP 까지 체결가로 바뀌었다.**
> 이 셋은 장부에 배당 칸이 없어 배당이 빠졌다 — N1 때 못 봤다. 7차에서 세 전략만 조정종가로 되돌렸다(근본 해결은 D8).

**브라우저로 잡은 것** — 회귀가 초록인데도 배당이 하나도 안 잡혔다.
`lastQuote` 를 **세 군데가 각자 손으로 만들고 있어서** 새 칸(`dividends`·`priceBasis`·
`daysAdj`·`ohlcAdj`)을 전부 떨어뜨리고 있었다. 엔진은 `lastQuote` 를 보므로
`isTradeBasis(Q)` 가 늘 false 였다. `mkLastQuote(q,SL,live)` 한 곳에서만 만들게 고쳤다.
**픽스처를 함수에 직접 넘기는 시험으로는 절대 못 잡는 종류다** — 실제로 페이지를 띄워
모의체결을 굴려 본 뒤에야 나왔다.

**검증** — 회귀 1667 PASS (새 절 `[101]` 무매 배당 장부 · `[102]` VR 배당 ·
`[103]` 모의체결기를 통째로 굴려 확인). 변이 시험 6건 전부 빨간불을 확인했다.
그중 `putDiv(d)` 를 주석 처리한 변이는 **정규식 시험을 그대로 통과했고**(`//putDiv(d);`
안에도 글자가 있다), 엔진을 실제로 굴리는 `[103]` 을 넣고서야 7건이 빨개졌다.

---

### N2 [HIGH · 수정 완료] `vrReplay` 가 끝을 안 잘랐다 — 미확정 봉까지 먹었다

```js
const D=bars.filter(d=>d.date>=from);   // 상한이 없다
```

- `bars` = `lastQuote.vr.ohlc` = `/api/quote?range=max` 응답 → **오늘 장중 봉이 들어 있다**
  (실측: `last.date = 2026-09-22`, 오늘 봉이 `ohlc` 마지막에 존재)
- `vrSimForward` 는 `_lastSettled(O, cur) || simCutoff(...)` 로 **확정 봉까지만** 자른다
- `vrAutoAdvance` 도 이번에 확정 봉 기준으로 고쳤다
- **재생만 안 자른다**

**영향**: 장중에 과거재생을 돌리면 아직 움직이는 오늘 고가·저가로 체결을 판정해
기록에 박고, `st.simLast = 오늘` 로 찍는다. 그 뒤 모의체결은 오늘을 건너뛴다.
→ 오늘 하루가 **미확정 값으로 굳는다.**

**수정 완료** — `const _cutR=_lastSettled(bars, curOf(st)) || simCutoff(curOf(st));` 로 끝을 자른다.
모의체결·자동진입·과거재생 셋이 같은 확정 마감 기준을 쓰는지 회귀 `[93]` 이 검사한다.

---

### N3 [MEDIUM] 익절 체결 모델이 한쪽은 설정, 한쪽은 하드코딩

| 경로 | 판정 |
|---|---|
| 백테 `runIM`/`runIM50` | `const _fillHit = (imFill==='high') ? hi : c;` — **사용자 설정**(기본 `high`) |
| 모의체결 `infSimForward` | `if(hi>=tgt ...)` — **하드코딩 `high`** (주석에 "백테 runIM(imFill=high)과 같은 규약" 이라 적혀 있지만 설정을 읽지는 않는다) |
| 운영 주문표 | 지정가 주문을 내보이기만 함 (판정 없음) |

기본값끼리는 같다. 그러나 백테에서 `imFill='close'` 를 고르면 **백테만 보수적으로 바뀌고
모의는 안 바뀐다** — 같은 세션인데 두 화면이 다른 모델이 된다.

> 감사 ⑤가 지적한 방향과 **반대로** 이미 한 번 바뀐 상태다.
> (감사 시점: 백테 high / 모의 close → 현재: 백테 설정 / 모의 high 고정)

**고칠 방향**: 체결 모델을 세션 설정으로 올리고 네 경로가 같은 값을 읽게 하거나,
백테 토글을 없애고 `high` 로 고정.

---

### N4 [MEDIUM · 수정 완료] `runVR` 이 공용 사다리를 안 썼다 — 사다리가 2벌이었다

- `vrLadder` 정의: index.html 1곳 · 호출: `vrSimForward`, `vrReplay` 2곳
- **backtest.html 에는 `vrLadder` 가 정의도 호출도 없다** (`grep -c` → 0)
- `runVR` 안에 `_ladder(hi, lo2)` 가 **따로** 있다 (`up/q`, `dn/q` 루프를 자체 구현)

**수정 완료** — 공용 엔진 `vrOrderPlan(S,P,bar)` 을 두 파일에 같은 몸으로 두고
네 경로가 전부 이것만 부른다. 백테의 자체 사다리는 지웠고, `_ladder` 는 공용 엔진이 낸
체결을 백테 장부(`_vsellQ`·`_vbuyQ` — 세무 원가·수수료)로 적용하는 얇은 껍데기만 남았다.
회귀 `[73]` 파리티는 **96조합**(모델2×모드3×식2×이어받기4×수수료2)으로 늘렸다.

---

### N5 [MEDIUM] V 갱신식이 3벌

| 위치 | 형태 |
|---|---|
| `computeNextV(c, ev)` (index) | 운영 화면·`vrStepCycle`·자동진입이 씀 |
| `vrReplay` 내부 | `V = V + pool/G + (cv−V)/(2√G) + addCycle` 인라인 |
| `runVR` 내부 | 같은 식 인라인 |

`vrReplay` 는 앱 안에 있으면서도 `computeNextV` 를 **안 쓴다.**

---

### N6 [⚠ 7차 정정 — #236 에서 번복됨] VR 사다리는 공식이 아니었다

> **7차 점검 정정** — 아래 결론(V 복귀가 공식)은 #235 에서 구현했다가 **#236 에서 되돌려졌다.**
> 지금 코드는 '사이클 고정 20차 · 1주 사다리' 다. 두 해석 중 무엇이 원문인지는 **아직 확인하지 못했다** (본문 D4).
> 예전엔 엔진 위 주석까지 'V 복귀가 공식' 으로 남아 코드와 정반대였다 — 7차에서 실제 동작대로 고쳤다.

`vrLadder` 는 체결마다 **현재 보유수량**으로 차수를 다시 계산한다.
매도 1주가 나가면 걸어둔 매수 차수 $85 → $94.44 로 바뀐다.
앱 설명은 "첫날 표를 걸고 2주 방치" 라 **설명과 구현이 다르다.**

실측 (SOXL 전 구간 · 원금 1만$ · G=10 · 밴드 15% · 비용 ON):

| 모드 | dynamic (현재) | fixed (첫날 고정) | 차이 |
|---|---|---|---|
| 거치 | 68,542.65$ · 3,057거래 | 68,102.33$ · 2,894거래 | −0.64% |
| 적립 | 43,336.38$ · 1,670거래 | 43,267.25$ · 1,585거래 | −0.16% |
| 인출 | 49,597.58$ · 2,340거래 | 51,315.30$ · 2,247거래 | **+3.46%** |

**결론** — 내가 fixed/dynamic 둘 중 하나로 좁게 본 것이 틀렸다. 5차 감사가 짚은 대로
**둘 다 공식이 아니다.** 공식 VR 5.0 은 밴드를 벗어나면 *평가금을 V로 되돌린다* —
1주씩 움직이는 게 아니다.

    V=10,000 · 보유 100주 · 밴드 15% · 고가 116
      공식  트리거 115 에 13주 매도 → 87주 × 115 = 10,005 ≈ V
      옛것  1주만 매도 → 99주 × 116 = 11,484 (V 로 안 돌아옴)

**수정 완료** — 공식(V 복귀)이 기본값이고, 1주 사다리는 `커스텀` 으로 분리해 UI에도
갈라 적는다. 실측 (SOXL 1500봉 · 1만$ · G10 · 밴드15% · 비용 ON):

| 모드 | 사다리(옛) | 공식(새) | 차이 |
|---|---|---|---|
| 거치 | 68,542.65 · 3,057거래 | 60,146.62 · **98거래** | −12.25% |
| 적립 | 43,336.38 · 1,670거래 | 38,224.89 · 201거래 | −11.79% |
| 인출 | 49,597.58 · 2,340거래 | 43,305.96 · 97거래 | −12.69% |

---

## 부록 D — 4·5차 감사분 수정 기록

| # | 내용 | 회귀 |
|---|---|---|
| ① | 리버스 매도 1주 강제 제거 (9주→0 · 10주→1 · 19주→0 · 20주→1) | `[90]` |
| ② | 주문수량 룩어헤드 — `imBuyQty` 로 4경로 통일 (전일100/오늘80/배정1000 → 10주) | `[91]` |
| ③ | 리버스 복귀를 `'리버스복귀'` 기록으로 저장 · 비매매 기록은 상태 불변 | `[92]` |
| ④ | `IM_BIG_DEFAULT=15` · `imBigPct()` 단일화 · 엔진 `bigOverride` 파라미터 | `[13]` 확장 |
| ⑥ | 선택 첫날 `prevC` 를 워밍업에서 찾는다 (`imPrevClose`) | `[94]` |
| ⑧ | `vrReplay` 가 실제 OHLC 로 체결 (없으면 알리고 멈춤) | `[93]` |
| ⑩⑭ | 진입일 장중 미진입 · 휴장일이면 다음 거래일 확정 종가 | `[95]` |
| ⑫ | `autoCyc` 표시를 실제 동작(모의 전용)과 일치 | `[96]` |
| ⑬ | 공식 진입일 이전 진입은 '커스텀 조기 진입' 으로 확인·기록 | — |
| ⑮ | 같은 날 양방향 체결은 매도→매수 가정임을 명시 | — |
| ⑯ | 실력공식을 '실험적 확장 · 원전 미확인' 으로 표시 | — |

**5차 감사분**

| # | 내용 | 회귀 |
|---|---|---|
| ① P0 | ~~VR 체결을 공식(V 복귀)으로~~ → **#236 에서 사다리로 되돌림** (7차 D4) · 네 경로 공용 엔진은 유지 | `[69]`·`[73]`·`[100]` |
| ② P0 | 리버스 쿼터매수 배정액 = **잔금÷4** (강제 1주 제거) | `[97]` |
| ③ P1 | VR 적립식 최초 자금을 세 엔진 통일 · 재생 CAGR 분모 정정 | `[99]` |
| ④ P1 | 복합거래도 최종 0주면 사이클 종료 | `[98]` |
| ⑤ P1 | 데이터 첫 봉 룩어헤드 — `imPrevClose` 가 0 반환, 그날 주문 없음 | `[94]` |
| ⑥ P2 | VR 실현손익 원가에 매수 수수료 포함 | `[75]` |
| N2 | `vrReplay` 도 확정 봉까지만 (미확정 봉 차단) | `[93]` |

---

### v3.83.0 — 앱을 체결가 + 배당 이벤트로 (N1)

| 고친 것 | 값으로 확인 |
|---|---|
| 로더가 `div=1` 로 받고 `days`·`ohlc` 를 체결가로 내보낸다 | 순수 함수 `quoteToDaily` 를 픽스처로 굴려 확인 · 체결가 없으면 조정 기준으로 |
| 무매 장부에 `'배당'` 기록 신설 | 잔금만 늘고 T·평단·보유·리버스 상태 불변 · 단리 인출 9,500 → 9,900 |
| VR 배당 자동 생성 (`type:'div'`) | Pool 입금 · 실효 평단이 배당만큼 내려감 · 명목 평단 불변 |
| 앱·백테 배당 금액 일치 | 같은 주수·같은 주당배당에서 세후 현금이 1e-9 이내로 같다 |
| 조정 기준이면 안 준다 | 앱·백테 둘 다 0 — 이중계상 방지 |
| `lastQuote` 를 한 함수에서만 만든다 | 세 곳이 각자 만들다 배당·가격기준을 떨어뜨리고 있었다 (브라우저로 재현) |

**아직 다른 점** — 백테의 '비용반영' 스위치를 끄면 배당에 세금을 안 뗀다(`costOn`).
앱은 늘 뗀다. 앱은 실계좌를 본뜨는 것이고 비용반영은 백테 전용 what-if 스위치라
일부러 두었다. 수수료도 같은 성격으로 이미 갈려 있다(앱 무매 0 · 앱 VR `IVS_FEE`).

---

## 부록 E — 자체 점검 1차 때의 GPT 확인 요청 (본문 7장으로 갱신됨)

1. **VR 사다리** — 사이클 첫날 주문표를 고정하는가(fixed), 체결마다 재계산하는가(dynamic)?
   → 3부 N6 의 숫자 참고. 원문 문장 인용 부탁.
2. **VR 실력공식** `V + Pool/G + (E−V)/(2√G) ± add` 의 원전 출처(문서·회차·페이지).
3. **VR 배당** — 원전이 분배금을 Pool 에 넣는가, 재투자인가, 아예 무시인가?
   (현재 백테는 Pool 에 넣고, 앱은 조정종가에 내재된 것으로 본다 — N1)
4. **무매 익절 체결** — 지정가 익절은 장중 고가 터치로 보는가, 종가 도달로 보는가?
   공식 V4.0 지정가매도는 프리장~애프터까지 유지되므로 정규장 일봉 고가도 근사다. (N3)
5. **무매 리버스 매수가** — 별지점 −$0.01 이 맞는가? gap% 변형의 원전 근거가 있는가?
6. **VR 적립식 첫 매수** — `initAmt` 인가 `startPool + contrib` 인가? (감사 ⑪ 미착수)

