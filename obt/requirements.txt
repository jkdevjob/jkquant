# 시초가 단타 백테스트 — 의존성
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
PyYAML>=6.0

# 선택(실데이터 수집용). 없으면 CSV/샘플데이터로 동작한다.
# pykrx>=1.0.45      # 한국거래소 일봉·거래대금 (분봉은 미제공)
# yfinance>=0.2      # 참고용 해외/일부 국내 시세
pytest>=7.0
