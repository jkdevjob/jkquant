# jkquant 작업 규칙

## 버전

버전은 **x.y.z** 형식을 쓴다.

| 자리 | 올리는 경우 |
|---|---|
| 마지막 (z) | 작은 수정 · 버그 수정 · UI 조정 |
| 가운데 (y) | 기능 추가 · 중간 규모 변경 |
| 첫 (x) | 전면 개편 · 호환성이 크게 바뀌는 수준 |

**배포할 때는 반드시 버전도 함께 갱신한다.**

버전이 적힌 곳 — 한 페이지 안에 여러 곳이면 값이 서로 같아야 한다.

| 파일 | 위치 |
|---|---|
| `index.html` | `#appVerTop` (머리말) · `#appVer` (설정창) — **두 곳 다** |
| `backtest.html` | `#btVer` |
| `admin.html` · `scalping.html` · `ipo.html` | 각 머리말 |

회귀 `[76]`이 형식과 페이지 내 일치를 검사한다.

## 배포 전 필수

```
node regression-check.js index.html backtest.html testdata
```

**ALL PASS 가 아니면 배포하지 않는다.** 테스트 개수가 많다는 이유로 완료 처리하지 않는다 —
고친 것마다 값으로 확인하는 시험을 같이 넣고, 옛 코드로 되돌리면 실제로 빨간불이 뜨는지
(변이 시험) 확인한다.

## 전략 코드 원칙

- **같은 걸 두 군데서 세지 않는다.** 운영·백테·과거재생이 같은 규칙을 각자 구현하면
  반드시 갈라진다. 실제로 VR이 그렇게 갈라졌다 — 공용 함수(`vrLadder`·`vrNextDue`)로 모았다.
- **저장 전 장부 == 저장 후 장부.** 엔진 안에서 계산한 값(수수료 등)이 거래이력에 안 남으면
  화면이 다시 계산할 때 갈린다. 기록이 스스로 말하게 한다(`fee`·`init`).
- **공식 규칙에 임의 판단을 섞지 않는다.** 근거가 없으면 '실험적 확장'으로 표시하거나
  아예 막는다. 공식 결과와 실험 결과가 같은 이름으로 보이면 안 된다.
- **룩어헤드 금지.** 종가 신호 전략은 전일까지 확정된 지표로 신호를 정하고 당일 종가에
  체결한다. 예약주문(무매 지정가·VR 사다리)만 당일 고가·저가 체결이 정상이다.
  회귀 `[71]`이 탐지한다.

## 작업 흐름

1. 버그는 바로 고친다. 기능 추가는 물어본다.
2. 버전 두 자리(또는 해당 페이지 전부) 갱신
3. 회귀 ALL PASS
4. 커밋 → 푸시 → PR → squash merge → 브랜치를 main에 맞춤
5. `curl`로 배포된 버전 확인
