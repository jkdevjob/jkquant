# ChatGPT 전략 구현 감사·수정 인계서 — 2026-09-20

## 0. 작업 원칙

- 전용 브랜치: `chatgpt/audit-fixes-20260920`
- 기준 브랜치: `main` (분기 당시 `d654ea741ca59b209cc71074e053eb00ddae446c`)
- `main`에는 직접 반영하지 않았다. Claude 작업과 충돌하지 않도록 모든 수정은 이 브랜치/PR #206에서만 진행한다.
- 사용자 제공 `무한매수법.txt`를 1차 기준으로 삼고, 공개 참고자료는 quantstack의 V2.2/V3.0/V4.0/VR 절차 페이지와 교차 확인했다.
- 공개 자료에 명확히 없는 세부 규칙은 임의로 “정답”으로 만들지 않았다. 그런 부분은 아래 제한사항에 따로 표시한다.

## 1. 심각도 높은 수정

### A. V4/V5 리버스 별지점 룩어헤드 제거

**문제**
리버스 별지점은 “직전 5거래일 종가 평균”이어야 하는데 백테스트가 오늘 종가를 먼저 history에 넣고 평균을 계산했다. 운영 주문표도 자동시세 배열의 마지막 5개를 그대로 써 장중 진행 중인 오늘 봉이 섞일 여지가 있었다.

**왜 영향이 큰가**
오늘 종가를 알아야만 계산 가능한 주문가로 오늘 매매를 판정하면 미래정보 사용이다. 특히 급락/급등일 리버스 매수·매도 시점이 달라져 성과가 직접 왜곡된다.

**수정**
- `runIM`, `runIM50`: 오늘 종가를 넣기 전에 `reverseStarPrev` 계산.
- `calcStarPoint`: `settledBars(...).slice(-5)`로 확정봉만 사용.
- 최근 5일 표시도 확정봉만 사용.
- 모의 `infSimForward`는 현재 행 직전 5개 봉만 사용.

### B. V4/V5 하루 주문을 장중 체결 결과로 재계산하던 오류

**문제**
같은 날 지정가/쿼터매도가 먼저 체결되면 바뀐 T·잔금·평단으로 그날 LOC 매수 주문을 다시 계산했다.

**원 규칙 의미**
오늘 주문은 하루 시작 전에 만들어 둔 주문이다. 장중에 매도가 먼저 체결됐다고 해서 이미 걸어 둔 같은 날 LOC 주문 가격/배정액을 새 T로 다시 만드는 것이 아니다.

**수정**
- 백테스트: `orderT/orderCash/orderAvg/orderHadShares`를 하루 시작 상태로 고정.
- 운영 모의: `const order=c, B=imBuy1(order)`로 같은 규약 적용.
- 매수선·1회매수금·전/후반 판정은 `order` 기준.

### C. V4 지정가 전량매도 직후 같은 날 LOC 재매수를 누락

**문제**
지정가매도로 수량이 0이 되는 순간 사이클을 끝내 버려, 종가가 LOC 매수선까지 내려오면 이미 걸려 있던 매수 주문이 체결될 수 있는 경우를 삭제했다.

**수정**
그날의 지정가매도·쿼터매도·LOC매수를 모두 처리한 뒤 최종 수량이 0일 때만 사이클 종료.

운영 모의의 `liquidated` 차단도 제거했다.

### D. V4.0 공식 기본값과 앱 기본값 불일치

**문제**
앱은 V4.0을 선택해도 리버스가 기본 OFF였다. OFF는 원금 소진 뒤 리버스 무한매도/쿼터매수를 하지 않는 변형인데 화면은 이것을 기본 V4.0처럼 취급했다.

**수정**
- 새 무매 세션 기본 `reverse:true`.
- 기존 세션에서 `reverse` 값이 아예 없으면 ON으로 해석.
- 명시적으로 `reverse:false`인 세션만 OFF 유지.
- 백테스트 `imReverse=true` 기본.
- UI에서 ON을 “V4.0 오피셜”, OFF를 “사용자 변형”으로 구분.
- 익절 동적조절은 별개 JK 확장이므로 계속 기본 OFF.

### E. 리버스 회복 후 일반모드 복귀 순서

**문제**
운영 모의는 회복 가격을 넘었어도 `분할-T >= 1` 조건까지 만족해야 리버스를 풀었다.

**수정**
회복가격을 넘으면 리버스 상태를 종료하고 **다음날 일반모드**로 간다. T는 그대로 승계한다. 다음날에도 1회분이 불가능하면 다시 리버스 1일차로 진입할 수 있다.

### F. 기존 모의 기록 재생성

V4 체결순서가 바뀌었는데 옛 모의 기록을 그대로 두면 화면에 과거 잘못된 체결이 남는다.

- `SIM_RULE_VER` 3 → 4.
- 규칙 지문이 바뀌어 기존 모의 기록을 새 체결 규칙으로 재생성하게 했다.

## 2. VR 5.0 수정

### A. 2주 주문을 10번째 거래일에만 검사하던 오류

**문제**
V/밴드는 10거래일마다 갱신하지만, 그 사이 예약한 LOC 주문은 2주 동안 살아 있어야 한다. 기존 백테스트는 `i % 10 === 0`인 날에만 체결을 검사했다.

**수정**
- V·밴드·사이클 매수한도 갱신: 10거래일마다.
- 그 사이 LOC 매수/매도 체결 여부: **매 거래일 검사**.
- 한 사이클 Pool 사용한도(75/50/25%)를 `buyBudget`으로 유지.

### B. 정수주 내림 잔액 소실

**문제**
예산 1,000달러로 정수주만 사서 실제 930달러가 체결돼도 Pool에서 1,000달러를 모두 없애는 경우가 있었다.

**수정**
`_vbuy`가 실제 체결액+수수료를 반환하고, Pool에서는 실제 사용액만 차감. 남은 현금은 Pool에 보존.

### C. VR MDD

거래·수수료·Pool을 반영한 일말 총자산으로 매 거래일 MDD를 계산하도록 유지/정리했다.

## 3. 무한매수 V2.2 수정

- T = 누적매수액/1회매수금, **소수점 셋째 자리 반올림(2자리)**.
- T 누적액에는 매수 수수료를 섞지 않는다.
- 후반전 별%가 음수가 되어도 1/4 별지점 LOC매도를 계속 유지.
- 쿼터손절 1회매수금은 기존 1회매수금보다 커지지 않게 상한.
- 쿼터 모드 10번째 **실제 체결된** 매수 뒤 변경된 평단으로 -10% 복귀 기준 재계산.
- 매수 주문이 0주라 체결되지 않았는데도 `qN++` 하던 가능성 제거.
- 1/4·3/4 매도수량을 정수주로 처리: `q1=floor(qty/4)`, `q3=qty-q1`.

## 4. 무한매수 V3.0 수정

- T는 소수점 둘째 자리 **올림하여 1자리** 사용.
- 양(+) 실현수익 누적 `profitCum`과 쿼터 대비 보관금 `reserve` 분리.
- 1회매수금은 `baseOne + profitCum/40`을 다음 거래일부터 반영하며 손실 때문에 줄이지 않음.
- 보관수익은 쿼터 모드에서 5회 고정분할 `qEach`.
- 1/4·3/4 매도수량 정수화.
- 공개 요약 사이에 5회 추가매수 이후 쿼터 탈출/MOC 반복의 세부 설명이 완전히 일치하지 않아 그 이후 규칙은 임의로 새로 만들지 않았다.

## 5. 시초가/단타 백테스트 수정

**문제**
다음 거래일이 손절가 아래로 갭하락해 시작해도 정확히 -4%에 손절된 것으로 계산했다. 목표가 위 갭상승도 정확히 +6%로 고정했다.

**수정**
- 진입 다음날 이후 시가가 손절/목표선을 통과해 갭으로 시작하면 **실제 시가 체결**.
- 같은 일봉에서 손절/목표가 둘 다 닿으면 기존 보수적 규약인 손절 우선 유지.
- UI 설명도 갭 시가 체결로 수정.

## 6. 단리(고정원금) 의미 정리

초기에 “손실 사이클은 잔고 그대로 이월”이라고 해석해 외부보충을 제거했으나, 현재 앱의 운영 장부는 단리를 **매 사이클 동일 원금으로 시작하는 고정원금 방식**으로 명시적으로 구현하고 있었다.

최종 결정:
- 초과익: 외부 인출(`savedProfit`).
- 원금 부족: 외부입금으로 보충(`addedCash`).
- 외부입금은 수익이 아니므로 최종성과에서 차감/현금흐름에 별도 기록.
- 코드 동작은 유지하고, “손실 이월”이라고 반대로 적혀 있던 백테스트/설정 UI 문구를 실제 규약과 일치시켰다.

이 부분은 사용자 제공 무한매수 원문에서 “단리”의 독립 정의가 충분히 상세하지 않으므로 **앱의 고정원금 비교 모드 정의**로 이해해야 한다.

## 7. DCA 재검토 — MDD는 유지, 하락배수 신호 룩어헤드 수정

현재 `_dcaOne`의 MDD는 적립금 유입을 수익으로 오인하지 않도록 **단위가치 = 평가액 / 누적투입** 경로로 계산하고 있었다. 분배금을 현금으로 받는 모드도 raw 가격 + 누적 현금분배 경로의 일별 `mddCash`를 사용하므로 MDD 계산 자체는 유지했다.

다만 “200일선 아래면 매수금 배수” 옵션은 **오늘 종가와 오늘 200일선으로 오늘 종가 매수 배수까지 결정**하고 있어 룩어헤드였다.

수정:
- 전일 확정 종가와 전일 200일선을 비교해 오늘 매수 배수를 결정.
- 오늘 종가는 체결가격으로만 사용.
- 200일선 워밍업 전에는 기존처럼 1배.

## 8. 추가 룩어헤드·회계 보정

### A. 표준편차(σ밴드) 전략
기존에는 오늘 종가로 σ밴드 위치와 200일선 상태를 판정한 뒤 같은 오늘 종가로 리밸런싱했다.

수정:
- **전일 확정 σ밴드/200일선 신호 → 다음 거래일 종가 체결**.
- `nobuy`/전량이탈 필터도 전일 신호를 사용.

### B. ASAP 전략
기존에는 오늘 종가·RSI·이평 크로스로 오늘 종가 매수를 결정해 룩어헤드가 있었다.

수정:
- RSI·200일선·5/20·20/50 크로스·하락장/재진입 상태를 전일 확정값으로 판정.
- 실제 매수는 다음 거래일 종가.
- 적립금 유입이 자산 고점을 인위적으로 높여 MDD를 줄이지 않도록 **현금흐름 중립 단위가치(NAV)** 로 MDD 계산.

### C. VR MDD
VR 적립식에서도 외부 적립금이 고점을 밀어 올려 MDD를 축소할 수 있어, 총자산 절대금액 대신 **현금흐름 중립 NAV 단위가치**로 낙폭을 계산하도록 보정했다. 인출식은 Pool→누적인출 이동이 총 회수가치를 바꾸지 않도록 같은 회수가치 기준을 유지한다.

### D. B&H 기준선의 정수주 잔액
거치식 기준선이 정수주 매수 후 남은 현금을 자산에서 누락했다.

수정:
- `cap - 실제 매수금 - 매수수수료`를 현금으로 보존.
- 일별 평가액·최종액·현금분배 raw 경로 모두 이 잔액을 포함.
- 기준선 자체가 정수주 내림 때문에 불필요하게 낮아지던 문제를 제거.

## 9. 남아 있는 명시적 한계 / 임의로 고치지 않은 부분

1. **V3.0 쿼터모드 5회 이후 세부 순서**
   - 확보한 공개 요약끼리 표현이 완전히 같지 않다.
   - 확실한 부분만 수정했고 이후 세부를 임의 확정하지 않았다.

2. **V4 리버스 쿼터매수 가격의 gap 기본 2.5%**
   - 원문 요약은 “별지점 아래에서 매수”를 명시하지만 정확히 2.5%가 공식 고정값이라는 근거는 현재 자료에서 충분하지 않다.
   - 앱에서는 사용자 조절 가능한 확장 파라미터로 유지한다.

3. **V4 공식 적용 종목**
   - 사용자 자료 기준 공식 설명의 핵심은 TQQQ/SOXL.
   - TECL/KORU 등은 앱 확장 적용이므로 UI에서도 비공식/자체 백테스트로 구분해야 한다.

4. **일봉 백테스트의 본질적 한계**
   - LOC/MOC/지정가의 실제 장중 순서를 일봉 OHLC만으로 완전히 재현할 수는 없다.
   - 명확히 알 수 없는 동시 터치에서는 보수적 규약을 유지한다.

5. **단리 고정원금**
   - 외부입금 보충은 앱의 비교모드 정의다. 무한매수 원전 자체의 필수 규칙으로 단정하지 않는다.

## 10. 회귀검사로 잠근 항목

- V4 지정가 전량매도 → 같은 날 LOC 재매수면 사이클 미종료.
- V4/V5 하루 주문기준 snapshot.
- V4 기본 리버스 ON (백테스트·새 운영세션·구세션 기본해석).
- 리버스 별지점은 확정된 직전 5거래일.
- VR 중간 거래일 LOC 체결.
- VR 정수주 잔액 Pool 보존.
- V2.2 T 반올림, 후반 LOC, 쿼터 1회금, 실제 매수 회차, 정수 매도수량.
- V3 T 올림, 반복리/보관금 분리, 5회분할, 정수 매도수량.
- 단타 갭하락 손절/갭상승 목표 시가 체결.
- 단리 UI가 외부입금 규약을 숨기지 않음.
- 모의 규칙 버전 4.

## 11. Claude 리뷰 우선순위

1. `backtest.html`: `runIM`, `runIM50`, `runVR`, `runIM22`, `runIM30`.
2. `index.html`: `defInfSettings`, `computeInf`, `calcStarPoint`, `renderOrder`, `infSimForward`, `SIM_RULE_VER`.
3. `scalping.html`: `btSimOne`.
4. `regression-check.js`: [3b], [4b], [4b-2], [4b-3], [11], [14], [19], [26] 및 이번 감사 추가 회귀.
5. PR #206의 diff를 main과 비교하고, 특히 “공식 규칙”과 “앱 확장” 표시가 섞이지 않았는지 확인.

## 13. 최종 검증 상태

- 전용 브랜치 HEAD: `188d6a67935d66001f7759109f22fadb8186870a`
- main 대비: **31 commits ahead / 0 behind**
- 변경 파일: `CHATGPT_AUDIT_20260920.md`, `backtest.html`, `index.html`, `regression-check.js`, `scalping.html`
- 전략 SSOT: **22 / 22 PASS**
- 전체 회귀: **931 PASS / 0 FAIL**
- GitHub Actions push 검증: **SUCCESS**
- GitHub Actions PR 검증: **SUCCESS**
- PR #206은 **draft**, main에는 merge하지 않음.

## 12. 공개 참고

- https://quantstack.app/infinite/v2-2/
- https://quantstack.app/infinite/v3-0/
- https://quantstack.app/infinite/v4-0-normal/
- https://quantstack.app/infinite/v4-0-reverse/
- https://quantstack.app/vr/procedure/
- https://quantstack.app/vr/overview/

quantstack 자체도 비공식 학습용 정리임을 밝히므로, 사용자 제공 원문과 충돌하는 경우 사용자 제공 자료를 우선했다.
